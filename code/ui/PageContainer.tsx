import { styled, View } from "tamagui";

export const PageContainer = styled(View, {
  w: "100%",
  maw: 600,
  mx: "auto",
  bg: "$color1",
  blw: 1,
  blc: "$borderColor",
  brw: 1,
  brc: "$borderColor",
});
